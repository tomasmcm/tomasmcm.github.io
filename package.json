{
  "name": "portfolio-2018",
  "version": "1.0.0",
  "main": "index.html",
  "license": "MIT",
  "scripts": {
    "start": "jekyll serve",
    "ipfs-build": "jekyll build && node dither.js && cd _site/ && npx all-relative"
  },
  "dependencies": {
    "gleech": "^0.1.0",
    "glob": "^8.0.3"
  }
}
